<%- include('../partials/head') %>
</head>
<body class="bg-rose-50 font-sans">

  <!-- Navbar -->
  <%- include('../partials/nav') %>

  <!-- Main Content -->
  <main class="container mx-auto px-4 py-12">
    <h1 class="text-4xl sm:text-5xl font-extrabold text-center text-rose-600 mb-12 drop-shadow-md animate-fade-up">
      You are on the Home Detail Page
    </h1>

    <!-- Home Card -->
    <div class="max-w-3xl mx-auto bg-white rounded-2xl shadow-lg overflow-hidden transform hover:scale-[1.02] hover:shadow-xl transition duration-300 animate-fade-up">
      <img src="<%= home.photourl %>" alt="Home Image" class="h-60 w-full object-cover">
      
      <div class="p-6 sm:p-8">
        <h2 class="text-2xl font-bold text-slate-800 mb-2"><%= home.housename %></h2>
        <p class="text-slate-500 text-sm mb-4">📍 <%= home.location %></p>
        
        <div class="flex justify-between items-center text-base text-slate-700 mb-6">
          <span class="font-medium">💰 ₹<%= home.pricepernight %> <span class="text-sm text-slate-500">/night</span></span>
          <span class="text-amber-500 font-semibold">⭐ <%= home.rating %>/5</span>
        </div>

        <a href="/book/<%= home._id %>" class="block w-full text-center bg-rose-500 hover:bg-rose-600 text-white font-semibold py-3 rounded-lg transition duration-300">
          Book Now
        </a>
      </div>
    </div>
  </main>

</body>
</html>

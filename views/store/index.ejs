<%- include('../partials/head') %>
<style>
  @keyframes fadeInUp {
    0% { opacity: 0; transform: translateY(20px); }
    100% { opacity: 1; transform: translateY(0); }
  }
  .animate-fade-up {
    animation: fadeInUp 0.6s ease-out forwards;
  }
</style>
</head>
<body class="bg-rose-50 font-sans">

  <!-- Navbar -->
  <%- include('../partials/nav') %>

  <!-- Main Content -->
  <div class="container mx-auto px-4 py-10">
    <h1 class="text-5xl font-extrabold text-center text-rose-600 mb-12 drop-shadow-md">Welcome to Airbnb Index</h1>

    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-8">
      <% rhouses.forEach(home => { %>
        <div class="bg-white rounded-2xl shadow-lg overflow-hidden transform hover:scale-105 hover:shadow-xl transition duration-300 animate-fade-up">
          <img src="<%= home.photourl %>" alt="Home Image" class="h-52 w-full object-cover">
          <div class="p-5">
            <h2 class="text-xl font-semibold text-slate-800 mb-1"><%= home.housename %></h2>
            <p class="text-slate-500 text-sm mb-2">📍 <%= home.location %></p>
            <div class="flex justify-between items-center text-sm text-slate-700 mb-4">
              <span>💰 ₹<%= home.pricepernight %>/night</span>
              <span class="text-amber-500 font-medium">⭐ <%= home.rating %>/5</span>
            </div>
            <a href="/book/<%= home._id %>" class="inline-block w-full text-center bg-rose-500 hover:bg-rose-600 text-white font-semibold py-2 px-4 rounded-lg transition duration-300">
              Book Now
            </a>
          </div>
        </div>
      <% }) %>
    </div>
  </div>

</body>
</html>
